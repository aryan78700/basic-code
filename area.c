#include<stdio.h>
int main()
{
    int area, l, b;
    printf("enter the length",l);
    scanf("%d",& l);
    printf("enter the breadth",b);
    scanf("%d",& b);
    area = l*b;
    printf("area of reactangle- %d", area);
    return 0;
}